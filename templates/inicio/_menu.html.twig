<div class="collapse navbar-collapse" id="navbarSupportedContent">

    <!-- Left -->
    <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
        <li class="nav-item ml-auto mb-2 mb-lg-0">
            <a class="nav-link link-alameda" href="{{ path('reserva_index') }}">
                Reservas
            </a>
        </li>
    </ul>

    <!--Right -->

    {% if is_granted('ROLE_USER') %}
        <ul class="navbar-nav ml-auto">

            <li class="nav-item dropdown" style="margin-right: 75px;">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    <img src="{{ app.user.avatarUrl(30) }}"
                         class="nav-profile-img rounded-circle img-fluid img-thumbnail" alt="{{ base.siteName }}">
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="{{ path('app_perfil') }}">Perfil</a>
                    {% if is_granted('ROLE_ADMIN_RESERVA') %}
                        <a class="dropdown-item" href="{{ path('app_registro_voluntario_reserva') }}">Agregar
                            Voluntario</a>
                    {% endif %}
                    {% if is_granted('ROLE_RESERVA') %}
                        <a class="dropdown-item" href="{{ path('celebracion_index') }}">Ver Celebraciones</a>
                         <a class="dropdown-item" href="{{ path('meta_base_edit', {'id': base.id}) }}">Editar metabase</a>
                        <a class="dropdown-item" href="{{ path('invitado_index') }}">Ver Invitados</a>
                        <a class="dropdown-item" href="{{ path('reserva_lectura') }}">Lectura QR</a>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="{{ path('app_registro') }}">Registrar Usuario</a>
                    {% endif %}
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Cerrar Sesión</a>
                </div>
            </li>
        </ul>
    {% endif %}
</div>
