<form method="post" action="{{ path('queue_transport_delete', {'id': queue_transport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ queue_transport.id) }}">
    <button class="btn">Delete</button>
</form>
