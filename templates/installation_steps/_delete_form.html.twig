<form method="post" action="{{ path('installation_steps_delete', {'id': installation_step.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ installation_step.id) }}">
    <button class="btn">Delete</button>
</form>
